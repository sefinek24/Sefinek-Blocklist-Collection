<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <% if (title) { %><title><%= title %></title><% } %>
    <% if (meta.description) { %><meta name="description" content="<%= meta.description %>"><% } %>
    <% if (meta.keywords) { %><meta name="keywords" content="<%= meta.keywords %>"><% } %>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <% if (process.env.NODE_ENV === 'production') { %>
        <% if (meta.siteName) { %><meta property="og:siteName" content="<%= meta.siteName %>"><% } %>
        <% if (meta.color) { %><meta name="theme-color" content="<%= meta.color %>"><% } %>
        <meta property="og:url" content="<%= process.env.DOMAIN %>>">

        <% if (canonical || canonical === '') { %>
            <link rel="canonical" href="<%= `${process.env.DOMAIN}${process.env.NODE_ENV === 'production' ? '' : `:${process.env.PORT}`}` %>">
        <% } %>
    <% } %>

    <% if (style) { %><link rel="stylesheet" href="/css/<%= style %>.css"> <% } %>
    <% if (libs === 'luxon') { %><script src="https://cdn.jsdelivr.net/npm/luxon@latest/build/global/luxon.min.js"></script><% } %>
</head>
<body>
    <div class="navbar">
        <ul>
            <li><a href="/" <%- subpage === 'home' ? 'id="selected"' : '' %>>🏠 Home page</a></li>
            <li><a href="/generated">📝 Blocklists</a></li>
            <li><a href="/update-frequency" <%- subpage === 'update-freqency' ? 'id="selected"' : '' %>>🎯 Update frequency</a></li>
            <li><a href="/explorer" <%- subpage === 'explorer' ? 'id="selected"' : '' %>>📚 Markdown files</a></li>
            <li><a href="/api" <%- subpage === 'api' ? 'id="selected"' : '' %>>💻‍ API for devs</a></li>
            <li><a href="/logs">📘 Git pull logs</a></li>
        </ul>

        <button class="menu-toggle">☰</button>
    </div>
