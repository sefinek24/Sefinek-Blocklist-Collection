<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>Update frequency - Sefinek Blocklists</title>
    <meta name="description" content="This subpage displays the update time of repositories containing the list of blocklists for your time zone. You can refer to it to set up a suitable cron job for updating the blocklist on your instance, such as Pi-hole.">
    <meta name="keywords" content="update, frequency, pi-hole, cron, time, timezone, date, sefinek, blocklist, collection">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <meta name="theme-color" content="#000003">
    <meta property="og:site_name" content="Made by Sefinek♥">
    <meta property="og:url" content="https://blocklist.sefinek.net">

    <link rel="stylesheet" href="/css/style.css">
    <script src="https://cdn.jsdelivr.net/npm/luxon@latest/build/global/luxon.min.js"></script>
</head>
<body>
    <div id="header">Blocklist Collection made by Sefinek - v<%= version %></div>
    <div id="text"><a href="https://github.com/sefinek24/Sefinek-Blocklist-Collection" target="_blank">>> ⭐ if you ❤️ <<</a></div>
    <hr>

    <div class="container" style="padding-top:10px">
        <div>
            <a href="/" title="/">< Back</a>
        </div>
    </div>

    <p>
        <b>Your timezone:</b> <span id="user-timezone"></span>
    </p>
    <p>
        <b>Remote (GitHub):</b> <span id="github"></span>
        <br>
        <b>Local (blocklist.sefinek.net):</b> <span id="remote"></span>
    </p>

    <code>
        This subpage displays the update time of repositories containing the list of blocklists for your time zone.<br>
        You can refer to it to set up a suitable cron job for updating the blocklist on your instance, such as Pi-hole.
    </code>

    <script>
        function showCronInfo() {
            const userTimezone = luxon.DateTime.local().zoneName;
            document.getElementById('user-timezone').innerHTML = userTimezone;

            const github = luxon.DateTime.fromISO('<%= cron.github %>').setZone(userTimezone);
            const remote = luxon.DateTime.fromISO('<%= cron.remote %>').setZone(userTimezone);

            document.getElementById('github').innerHTML = github.toLocaleString(luxon.DateTime.DATETIME_MED);
            document.getElementById('remote').innerHTML = remote.toLocaleString(luxon.DateTime.DATETIME_MED);
        }

        window.onload = showCronInfo;
    </script>
</body>
</html>
